# 機密設定(絕不進版控)——對照 site/api/config.sample.php 自行建立,實際值放主機 / Actions secrets
/site/api/config-tg.php
/site/api/config.php

# 名單/資料備援(執行期產生)
/site/api/_data/*.csv

# 部署腳本(含主機座標 IP/使用者/路徑——公開 repo 不放,本機自用)
/deploy.sh
*.sock

# 系統雜檔
.DS_Store
*.swp
*~

# Python 工具快取(tools/*.py 執行時產生)
__pycache__/
*.pyc

# build 產物(若有)
/build/
